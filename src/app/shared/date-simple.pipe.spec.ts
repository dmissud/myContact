import { DateSimplePipe } from './date-simple.pipe';

describe('DateSimplePipe', () => {
  it('create an instance', () => {
    const pipe = new DateSimplePipe();
    expect(pipe).toBeTruthy();
  });
});

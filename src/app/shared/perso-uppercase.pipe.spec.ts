import { PersoUppercasePipe } from './perso-uppercase.pipe';

describe('PersoUppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new PersoUppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
